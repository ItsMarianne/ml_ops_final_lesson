# Pinned to match the versions uv.lock resolves for the `webapp` extra
# (`uv export --extra webapp`), trimmed by hand to exclude housing-ml's base
# dependencies (pandas, scikit-learn, pydantic, ...) -- this app never
# imports housing_ml, it only sends/receives JSON over HTTP, so it doesn't
# need any of that.
flask==3.1.3
requests==2.34.2
blinker==1.9.0
click==8.4.2
itsdangerous==2.2.0
jinja2==3.1.6
markupsafe==3.0.3
werkzeug==3.1.8
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0
colorama==0.4.6 ; sys_platform == 'win32'
